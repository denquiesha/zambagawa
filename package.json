{
  "name": "zambagawa",
  "version": "1.0.0",
  "private": true,
  "description": "A database-backed local services booking MVP for Zambales.",
  "scripts": {
    "check": "node --check app.js && node --check api/_supabase.js && node --check api/providers.js && node --check api/bookings.js && node --test"
  },
  "engines": { "node": ">=20" }
}
